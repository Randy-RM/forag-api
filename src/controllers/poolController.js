// Retrieve all posts from the database.
async function getAllPools(req, res, next) {
  return 0;
}

// Retrieve all posts from the database.
async function getAllPoolsOfUser(req, res, next) {
  return 0;
}

// Find a single post with an id
async function getOnePoolById(req, res, next) {
  return 0;
}

// Find a single post with an id
async function getOnePoolOfUserById(req, res, next) {
  return 0;
}

// Create and save a new post
async function createPool(req, res, next) {
  return 0;
}

// Update a post by the id in the request
async function updatePool(req, res, next) {
  return 0;
}

// Delete a post with the specified id in the request
async function deletePool(req, res, next) {
  return 0;
}

// Delete all posts from the database.
async function deleteAllPool(req, res, next) {
  return 0;
}

// Delete all posts from the database.
async function deleteAllPoolOfUser(req, res, next) {
  return 0;
}

// Find all published posts
async function findAllPoolPublished(req, res, next) {
  return 0;
}

module.exports = {
  getAllPools,
  getAllPoolsOfUser,
  getOnePoolById,
  getOnePoolOfUserById,
  createPool,
  updatePool,
  deletePool,
  deleteAllPool,
  deleteAllPoolOfUser,
  findAllPoolPublished,
};
