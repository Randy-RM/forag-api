const authRouter = require('./auth.routes');
const userRouter = require('./user.routes');
const poolRouter = require('./pool.routes');

module.exports = {
  authRouter,
  userRouter,
  poolRouter,
};
